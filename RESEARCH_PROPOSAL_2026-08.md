# Curriculum proposal: Senior Machine Learning Engineer — 2026-08

Generated by `aicg org research --apply`. Caps:

- `max_modules_per_run`: 1
- `max_exercises_per_run`: 3
- `max_projects_per_run`: 0
- `min_evidence_count`: 3 job postings per item

## Rationale

Research cycle sampled 26 in-window Senior ML Engineer postings across AI-native, consumer-tech, and fintech/health/adtech/enterprise segments (see .aicg/job-requirements.json postings array and JOB_REQUIREMENTS.md frequency table). Every existing module (mod-301 through mod-310) is validated by a frequency at or above the 0.30 in-scope threshold. No net-new requirement theme cleared all three continuity-bias gates simultaneously — (a) >=3 postings, (b) frequency >=0.30, (c) not incrementally extendable inside an existing module. The two closest emerging themes are (i) ranking / recommender systems at ~0.27 (7/26 postings — Reddit p010, Pinterest p013/p014, Instacart p015, Discord p017, Nextdoor p018, DoorDash p016) which sits neatly inside mod-303's existing 'modeling-regime-decision-rubric' exercise and does not warrant a new module, and (ii) agentic AI orchestration at ~0.19 (5/26 postings — Scale AI p003/p004, Cresta p007/p009, Stripe p019) which stays below threshold and is already covered by mod-304's delegation contract, with depth owned by the senior-agentic-ai-engineer-learning peer track at level 40. Distributed-training depth (GPU/CUDA/speculative decoding/QAT) surfaces at ~0.19 but is concentrated in AI-native inference-shop roles where the specialty *is* the role — that depth is owned upstream by training-pipeline-engineer and ai-infra-performance-learning. AI coding-assistant fluency (Cursor/Copilot/Codex) appears in 3 postings (0.12) as a preferred qual, well below threshold. The market has not shifted enough in the last 90 days to justify additions. Empty arrays below are the expected output — do not pad.

## Accepted

### Modules

_None._

### Exercises

_None._

### Projects

_None._

## Rejected

_None._

---

## Approval

**This proposal does NOT add anything to the curriculum automatically.**

If you approve some/all of the accepted items, merge this PR. After merge,
run `aicg org promote-plan --role <role>` (or wait for the next weekly
audit) to apply the filtered delta to `curriculum-plan.json` and scaffold
skeletons.

If you reject everything, close the PR. No further action needed; the
next research run will repropose anything still warranted by the job
market.
